#!/usr/bin/python3
# change in memeory address not with value
state1 = 'Tamil nadu'
state2 = 'Tamil nadu'

print (state1 is state2)
print (id(state1))
# is = compares the memeory address of the given identifier 

print (state1 == state2)
print (id(state2))
# == compares the value of the given identifier
# immutable datatybes
#float, integer, boolen, string, byte, complex
